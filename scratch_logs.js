const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); const componentCounts = {}; page.on('console', msg => { const text = msg.text(); if (text.includes('[RENDER]')) { const match = text.match(/\[RENDER\] (.*?)(: \d+)?$/); if (match) { const comp = match[1]; componentCounts[comp] = (componentCounts[comp] || 0) + 1; } } }); console.log('Navigating to http://localhost:3000 ...'); await page.goto('http://localhost:3000', { waitUntil: 'networkidle2' }).catch(e => console.log('Goto error', e)); console.log('Waiting 10 seconds to collect logs...'); await new Promise(resolve => setTimeout(resolve, 10000)); console.log('--- RENDER COUNTS ---'); for (const [comp, count] of Object.entries(componentCounts)) { console.log(comp + ': ' + count); } console.log('---------------------'); await browser.close(); })();
